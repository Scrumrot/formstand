node_modules
dist
coverage
*.log

# MUI version-matrix scratch output (cli/matrix — the package.json and
# lockfile ARE committed; the generated forms are per-run scratch)
cli/matrix/.generated/
.DS_Store
.vscode
.idea
docs/.vitepress/dist
docs/.vitepress/cache

# Personal scratch space — working notes, not part of the playground
# (remove this ignore when a recipe graduates into the app)
examples/src/recipes/

# e2e screenshot output (uploaded as CI artifacts, never committed)
examples/e2e/artifacts/
