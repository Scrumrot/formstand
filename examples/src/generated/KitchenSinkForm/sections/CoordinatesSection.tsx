// Generated by formstand-cli — edit freely, this file is yours.
import { numberInputProps } from "formstand";
import { useKitchenSinkField, useKitchenSinkIsDirty, useKitchenSinkIsValid } from "../hooks";

export type CoordinatesSectionProps = Readonly<{ heading?: string }>;

// The section hook is the path-scoped flags: dirty/valid for this
// subtree only, as boolean-only subscriptions.
export const useCoordinatesSection = () => ({
  dirty: useKitchenSinkIsDirty("coordinates"),
  valid: useKitchenSinkIsValid("coordinates"),
});

export const CoordinatesSection = ({
  heading = "Coordinates",
}: CoordinatesSectionProps) => {
  const { dirty, valid } = useCoordinatesSection();
  const element0 = useKitchenSinkField("coordinates.0");
  const element1 = useKitchenSinkField("coordinates.1");
  return (
    <fieldset>
      <legend>
        {heading}
        {dirty ? " — edited" : ""}
        {valid ? "" : " — has errors"}
      </legend>
      <div>
        <label>
          {"Coordinates 1"}
          <input {...numberInputProps(element0)} />
        </label>
        {element0.error?.[0] !== undefined ? (
          <p role="alert">{element0.error?.[0]}</p>
        ) : null}
      </div>
      <div>
        <label>
          {"Coordinates 2"}
          <input {...numberInputProps(element1)} />
        </label>
        {element1.error?.[0] !== undefined ? (
          <p role="alert">{element1.error?.[0]}</p>
        ) : null}
      </div>
    </fieldset>
  );
};
