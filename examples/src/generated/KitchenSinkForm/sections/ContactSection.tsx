// Generated by formstand-cli — edit freely, this file is yours.
import { useKitchenSinkIsDirty, useKitchenSinkIsValid } from "../hooks";
import { EmailField } from "../fields/EmailField";
import { PhoneField } from "../fields/PhoneField";
import { StreetField } from "../fields/StreetField";
import { CityField } from "../fields/CityField";
import { ZipField } from "../fields/ZipField";

export type ContactSectionProps = Readonly<{ heading?: string }>;

// The section hook is the path-scoped flags: dirty/valid for this
// subtree only, as boolean-only subscriptions.
export const useContactSection = () => ({
  dirty: useKitchenSinkIsDirty("contact"),
  valid: useKitchenSinkIsValid("contact"),
});

export const ContactSection = ({
  heading = "Contact",
}: ContactSectionProps) => {
  const { dirty, valid } = useContactSection();
  return (
    <fieldset>
      <legend>
        {heading}
        {dirty ? " — edited" : ""}
        {valid ? "" : " — has errors"}
      </legend>
      <EmailField />
      <PhoneField />
      <fieldset>
        <legend>{"Address"}</legend>
        <StreetField />
        <CityField />
        <ZipField />
      </fieldset>
    </fieldset>
  );
};
