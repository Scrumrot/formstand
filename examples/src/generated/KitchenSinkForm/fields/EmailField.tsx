// Generated by formstand-cli — edit freely, this file is yours.
import { textInputProps } from "formstand";
import { useKitchenSinkField } from "../hooks";

export type EmailFieldProps = Readonly<{ label?: string }>;

export const useEmailField = () => useKitchenSinkField("contact.email");

export const EmailField = ({
  label = "Email",
}: EmailFieldProps) => {
  const field = useEmailField();
  return (
    <div>
      <label>
        {label}
        <input {...textInputProps(field)} />
      </label>
      {field.error?.[0] !== undefined ? (
        <p role="alert">{field.error?.[0]}</p>
      ) : null}
    </div>
  );
};
