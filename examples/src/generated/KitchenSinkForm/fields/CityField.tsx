// Generated by formstand-cli — edit freely, this file is yours.
import { textInputProps } from "formstand";
import { useKitchenSinkField } from "../hooks";

export type CityFieldProps = Readonly<{ label?: string }>;

export const useCityField = () => useKitchenSinkField("contact.address.city");

export const CityField = ({
  label = "City",
}: CityFieldProps) => {
  const field = useCityField();
  return (
    <div>
      <label>
        {label}
        <input {...textInputProps(field)} />
      </label>
      {field.error?.[0] !== undefined ? (
        <p role="alert">{field.error?.[0]}</p>
      ) : null}
    </div>
  );
};
