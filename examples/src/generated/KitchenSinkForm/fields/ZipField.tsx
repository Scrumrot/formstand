// Generated by formstand-cli — edit freely, this file is yours.
import { textInputProps } from "formstand";
import { useKitchenSinkField } from "../hooks";

export type ZipFieldProps = Readonly<{ label?: string }>;

export const useZipField = () => useKitchenSinkField("contact.address.zip");

export const ZipField = ({
  label = "Zip",
}: ZipFieldProps) => {
  const field = useZipField();
  return (
    <div>
      <label>
        {label}
        <input {...textInputProps(field)} />
      </label>
      {field.error?.[0] !== undefined ? (
        <p role="alert">{field.error?.[0]}</p>
      ) : null}
    </div>
  );
};
