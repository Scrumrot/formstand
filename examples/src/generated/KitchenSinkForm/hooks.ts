// Generated by formstand-cli — edit freely, this file is yours.
import { createForm, createFormHooks } from "formstand";
import { kitchenSinkSchema } from "./schema";
import type { KitchenSinkValues } from "./types";

// A form starts blank: required numbers/dates/enums begin
// undefined, so these initial values intentionally do not satisfy
// the schema yet; hence the cast. Validation reports the gaps on
// submit.
const initialValues = {
  title: "",
  contact: {
    email: "",
    phone: undefined,
    address: {
      street: "",
      city: "Springfield",
      zip: "",
    },
  },
  age: undefined,
  rating: null,
  newsletter: false,
  birthday: null,
  plan: undefined,
  channel: undefined,
  coordinates: [undefined, undefined],
  aliases: [],
  projects: [],
} as unknown as KitchenSinkValues;

// One module-level form for the whole feature; every file below imports
// these pre-wired hooks instead of receiving `form` via props or a
// provider. A module singleton never unmounts — for a per-mount
// lifecycle, use useForm + createFormContext instead.
export const kitchenSinkForm = createForm(kitchenSinkSchema, {
  initialValues,
  mode: "onBlur",
});

export const {
  useKitchenSinkField,
  useKitchenSinkFieldArray,
  useKitchenSinkIsDirty,
  useKitchenSinkIsValid,
  useKitchenSinkIsSubmitting,
} = createFormHooks(kitchenSinkForm, "kitchenSink");
