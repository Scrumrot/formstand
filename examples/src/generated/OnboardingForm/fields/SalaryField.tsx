// Generated by formstand-cli — edit freely, this file is yours.
import { TextField } from "@mui/material";
import { useMuiNumberFieldProps } from "../adapter";
import { useOnboardingField } from "../hooks";

export type SalaryFieldProps = Readonly<{ label?: string }>;

export const useSalaryField = () => useOnboardingField("employment.salary");

export const SalaryField = ({
  label = "Salary",
}: SalaryFieldProps) => {
  const field = useSalaryField();
  const fieldNumberProps = useMuiNumberFieldProps(field);
  return (
    <TextField fullWidth label={label} {...fieldNumberProps} />
  );
};
