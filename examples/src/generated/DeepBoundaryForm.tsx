// Generated by formstand-cli — edit freely, this file is yours.
import { z } from "zod";
import {
  DateField,
  NumberField,
  SelectField,
  TextField,
  useForm,
  useIsSubmitting,
} from "formstand";
import { deepBoundarySchema } from "./boundarySchemas";

type FormValues = z.input<typeof deepBoundarySchema>;

// A form starts blank, and every field here has a legal blank state
// (strings "", booleans false, optional/nullable undefined/null),
// so the draft typechecks as-is; no cast needed.
const initialValues: FormValues = {
  l1: {
    l2: {
      l3: {
        l4: {
          l5: {
            l6: {
              l7: {
                l8: {
                  leaf: "",
                  count: null,
                  l9: {
                    tooDeep: "",
                  },
                },
              },
            },
          },
        },
      },
    },
  },
  mixed: {
    optionalDate: undefined,
    nullableEnum: null,
    defaultedNumber: 42,
    piped: "",
  },
};


export const DeepBoundaryForm = () => {
  const form = useForm(deepBoundarySchema, { initialValues, mode: "onBlur" });
  const submitting = useIsSubmitting(form);

  return (
    <form
      onSubmit={form.handleSubmit((data) => {
        console.log("submit", data);
      })}
    >
      <fieldset>
        <legend>{"L1"}</legend>
        <fieldset>
          <legend>{"L2"}</legend>
          <fieldset>
            <legend>{"L3"}</legend>
            <fieldset>
              <legend>{"L4"}</legend>
              <fieldset>
                <legend>{"L5"}</legend>
                <fieldset>
                  <legend>{"L6"}</legend>
                  <fieldset>
                    <legend>{"L7"}</legend>
                    <fieldset>
                      <legend>{"L8"}</legend>
                      <TextField form={form} path={"l1.l2.l3.l4.l5.l6.l7.l8.leaf"} label={"Leaf"} />
                      <NumberField form={form} path={"l1.l2.l3.l4.l5.l6.l7.l8.count"} label={"Count"} />
                      {/* TODO: path "l1.l2.l3.l4.l5.l6.l7.l8.l9" exceeds formstand's typed FieldPath depth (9); bind by hand */}
                    </fieldset>
                  </fieldset>
                </fieldset>
              </fieldset>
            </fieldset>
          </fieldset>
        </fieldset>
      </fieldset>
      <fieldset>
        <legend>{"Mixed"}</legend>
        <DateField form={form} path={"mixed.optionalDate"} label={"Optional Date"} />
        <SelectField
          form={form}
          path={"mixed.nullableEnum"}
          label={"Nullable Enum"}
          placeholder={"Select nullable enum"}
          options={[
            { value: "a", label: "A" },
            { value: "b", label: "B" },
          ]}
        />
        <NumberField form={form} path={"mixed.defaultedNumber"} label={"Defaulted Number"} />
        <TextField form={form} path={"mixed.piped"} label={"Piped"} />
      </fieldset>
      <button type="submit" disabled={submitting}>
        {submitting ? "Submitting..." : "Submit"}
      </button>
    </form>
  );
};
