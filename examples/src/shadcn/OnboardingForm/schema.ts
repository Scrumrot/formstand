// One source of truth: the schema, option lists, and blank draft are
// shared with the plain Onboarding module, so the three kit variants
// can't drift.
export * from "../../forms/OnboardingForm/schema";
